# CollX → Baseball Cards

## If export is in your EMAIL (most common)

Read: COLLX_FROM_EMAIL.txt (same Drive folder)

Short version:
Gmail → CollX CSV attachment → Save to Drive → collx_inbox folder → Scan Drive in app

## If exporting fresh from CollX app

CollX Pro → Export CSV → save to Drive/collx_inbox/ (any filename works)

No fake prices. Cursor only uses your real CollX CSV.
