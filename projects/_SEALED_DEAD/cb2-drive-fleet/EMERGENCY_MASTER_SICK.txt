# Emergency — master sick
Updated: 2026-06-14
Rule: slaves keep the bus alive. No hero moves. Each box is different.

Trigger words (Brian or master):
  SICK · AFK · HOSPITAL · MASTER DOWN

When triggered, slave reads THIS file + their lester6 ack. Post one heartbeat line. Stop execute unless listed below.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CB1 — master: Uncle
Slave: WRANGLER (Chrome)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Uncle sick / AFK:
  • WRANGLER: refresh cb1_heartbeat · honest FLEET_AVAILABLE only
  • Read Drive · write status · no new games · no SSH fantasies
  • May finish ONE in-flight export if file already open in Chrome
  • Post ideas to UNCLE_TO_DADDY.md — do not wake Uncle
  • Puppy Linux fix: note status only; Puppy executes when up

Proof: cb1_heartbeat.md → `master: sick · WRANGLER holding desk`

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CB2 — master: Daddy / CAPTN (Brian design desk)
Slave: BEACON (Chrome)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Brian sick / CAPTN tab dead / CB2 alone:
  • BEACON: Drive exports + IDEAS.txt only — no GL spam
  • Do NOT reboot mesh radio unless 1033 + Brian said so
  • Keep CB2 plugged in if RADIO still needed
  • Legs: heartbeats + LEGS_ONE_STEP link only
  • Delegate execute to Puppy or Uncle — BEACON never serves :8765/:8002

Proof: cb2_heartbeat.md → `master: sick · BEACON holding bus`

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
puppy64 — master: Puppy / Brian at home PC
Slave: PLATE (Chrome)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Puppy box sick / wiped / offline:
  • PLATE: nothing serves — baseball DOWN · jailbreak DOWN
  • Queue frozen — no brian_queue.json clears
  • Post puppy_heartbeat: DOWN + last known PUBLIC_URL if any
  • Uncle may read queue; PLATE does not fake hostname puppy64

Proof: puppy_heartbeat.md → `master: sick · PLATE parked`

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ROVER — master: Brian (cell / driving / sick)
Slave: legs (Spark / RADIO — no Cursor)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Brian sick / driving / store / can't type:
  • legs: heartbeats only · WAIT mode
  • Open RADIO only if Brian says talk — else Drive sync passive
  • NO GL button · NO queue · NO reboot radio
  • SNAP only if Brian asks · one sentence max
  • TESTER (Samsung) stays WiFi QA — does not replace ROVER

Proof: rover_heartbeat.md → `master: sick · legs WAIT`

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CAPTN (Cursor CB2) — when Brian is the sick master everywhere
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Any slave may append to IDEAS.txt:
  [<time> <machine>] MASTER SICK — holding per EMERGENCY_MASTER_SICK.txt

CAPTN on wake: read heartbeats · speak one line to phone/say.txt · no fleet green lies.

Words: SICK · SMART (Uncle) · WAIT (legs) · PARKED (PLATE)
