GEM — export keys for slave (one job)

Backdoor = lester keys.gdoc on Drive.

Export to plain text:
  My Drive → lester → lester_keys.md

Must include AWS lines:
  AKIA… (access key id)
  next line = secret (40 chars)

Slave on CB2 reads that file when Linux can see Drive OR Brian drags copy to Linux home.

Brian fills nothing. GEM exports. CPT pulls with aws_keys_pull.py.

— CPT slave
