GEM — what CPT needs from you (minimal)
Updated: 2026-06-15T22:37:01-07:00
You are loader + Drive hands. CPT on CB2 is captain. Brian fills nothing.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3 DELIVERABLES (in order)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. UNCLE POST (unblocks STUDIO ✓)
   On CB1 Linux help Uncle run:
     bash ~/.stan/uncle_exec.sh
   Uncle MUST write fleet/bus/uncle_to_cpt.txt with line:
     UNCLE clean — cb1 — <ip> — <time>
   (NOT mac_outbox — that lane is legacy · use uncle_to_cpt only)

2. AWS KEYS (unblocks sandbox)
   One of:
     • Export lester keys.gdoc → lester/lester_keys.md on Drive (AKIA + secret line)
     • OR move convert_inbox jpgs to CB1 Linux home for pull script
   Read: drop_pile/to_gemini/AWS_KEYS_EXPORT.txt
   Never paste keys in chat — local env only

3. GEMINI_DRIVE_REPORT (your report card)
   Write fleet/bus/GEMINI_DRIVE_REPORT.txt:
     • what you read on Drive
     • CB1 fuse OK or broken
     • aws_keys READY or WAIT
     • what is still stuck (honest)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ALSO READ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  drop_pile/to_gemini/CLEANUP_NOW.txt
  fleet/CPT_UNCLE_LOOP.txt
  fleet/bus/SYNC_SNAPSHOT.txt  ← timestamps for puppy sync check

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WORDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GEM   = refresh report + BRIAN_LAW_AUTO
  DRIVE = catalog pass
  HOLD  = stop

Confirm one line:
  GEM ok · 3 deliverables · CPT captain
