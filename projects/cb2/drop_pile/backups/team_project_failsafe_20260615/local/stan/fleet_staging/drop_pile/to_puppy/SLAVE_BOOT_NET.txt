# Puppy — after wipe
# GEMINI: loader only. Do NOT make Gemini NET's LLM. Cursor = NET.
# Law: fleet/NET_LAW.txt

mkdir -p ~/.stan
echo 5f54badb > ~/.stan/slave.key
chmod 600 ~/.stan/slave.key
cp ~/GoogleDrive/MyDrive/lester/local_slave.py ~/.stan/ 2>/dev/null || true
# Open Cursor on puppy64 — word NET (not Gemini)
python3 ~/.stan/local_slave.py NET

Word: NET
