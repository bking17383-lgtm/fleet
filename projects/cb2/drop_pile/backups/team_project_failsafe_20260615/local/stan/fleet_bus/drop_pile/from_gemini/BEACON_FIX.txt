BEACON is retired → use CPT_SLAVE

GEM/slave on CB2 run:

  python3 ~/.stan/local_slave.py CPT_SLAVE

Or just:

  bash ~/.stan/slave_boot.sh

Same key in ~/.stan/slave.key (f770…). BEACON = old Lester name for CB2.

Word for Cursor: SLAVE or CPT — not BEACON.
