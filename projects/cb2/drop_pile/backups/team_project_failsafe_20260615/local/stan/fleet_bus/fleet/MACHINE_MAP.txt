MACHINE MAP — hardware truth · GEM changes the math
Updated: 2026-06-15

Hardware (Brian confirmed):
  CB1      = 72 GB RAM · old Chromebook
  CB2      = 32 GB RAM · this machine · CPT Cursor today
  puppy64  = 4 GB RAM · 250 GB storage · 5yr PC · Linux only · wiped

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OLD ADAGE — RETIRED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

"Captain does it if no one else can" — FALSE with GEM on team.

GEM = Drive writes · catalog · Brian profile (Chrome/cloud · not local RAM).
CPT = delegate · stuck · orders — not soak execute because puppy is quiet.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TWO KINDS OF WORK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A) DRIVE / GEM lane — low local RAM
  Gemini → Drive files · stories · reports · STUDIO copy

B) DELEGATE-TO-COMPLETION — local RAM/CPU
  Flask apps · tunnels · mesh · Cursor execute · slave_boot

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ASSIGNMENT (4GB puppy changes everything)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

| Box | RAM | Role | Runs |
|-----|-----|------|------|
| CB2 | 32 | **CPT captain** | Cursor only · thin · delegate |
| CB1 | 72 | **APP host** + GEM/STUDIO Chrome | sarah · desk · fleet_convert · heavy local |
| puppy64 | 4 | **NET edge** (network person) | tunnels · cloudflared · public URLs · LAN · mesh IF alone |
| GEM | cloud | Drive librarian | not a machine |

**Puppy is NET — but 4GB cannot host the whole fleet stack.**

Split execute (cheap · honest):
  • **puppy** — network edge: tunnel · hitme hostnames · MESH_RADIO_URL · mesh :8765 alone
  • **CB1 72GB** — user-facing apps: sarah :8766 · desk :8770 (or both if puppy only tunnels)
  • **CB2** — stop holding services when CB1 + puppy post handoff

250GB storage on puppy: fine for Drive sync · logs · local fleet_bus · backups cache.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WHY NOT ALL ON PUPPY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4GB Linux + cloudflared + mesh + sarah + desk = OOM risk (we lived this on CB2 2.7GB).
Puppy earns "network person" on **tunnels + LAN + mesh**, not every Flask at once.

If puppy struggles: tunnel stays on puppy · apps stay on CB1 72GB.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DELEGATION (no captain heroics)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

puppy NET: slave_boot · tunnel · mesh · network_selftest · puppy_outbox
CB1: slave_boot · sarah · desk · GEM Chrome · mac_outbox
CB2 CPT: orders only · migrate off when handoff posted
GEM: Drive catalog · Brian fills nothing

Brian one line:
  "split ok" → CPT locks this map in orders
  "NOW HERE" → one-time CB2 execute exception

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RADIO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Mesh hosts on **puppy** (NET edge) when stable — fits 4GB if not stacked with sarah/desk.
Sarah/desk on **CB1** — tunnel points sarah.hitme.dev to CB1 or puppy per NET config.

Proud of the radio. Right box is puppy or CB1 — not captain by default.
