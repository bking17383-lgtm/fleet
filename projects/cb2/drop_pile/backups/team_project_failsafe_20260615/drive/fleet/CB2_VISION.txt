CB2 VISION — what actually works
Updated: 2026-06-14

Your Chromebook: NO built-in camera in Linux. NO display toggle found.
GL is right: external camera OR workarounds below.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
USE NOW (terminal / Cursor)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

COPY  → copy text in Chrome (Ctrl+C)
        CAPTN reads clipboard

SNAP  → Chrome OS screenshot → upload to Drive:
        drop_pile/from_brian/snaps/
        then say SNAP in Cursor

WRITE → brian_says.txt on Drive (phone or any device)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CAMERA OPTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PLUG  → USB webcam into Chromebook
        then Gemini Live in CHROME (not Linux)
        EYES word still applies

PHONE → use phone camera in Gemini app (LIVE)
        best if no USB cam

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WHY LINUX SCREEN IS BLACK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Linux only sees its own empty desktop — not Chrome.
scrot/grim cannot capture Chrome OS windows on this box.
"Allow Linux display" may not exist on this model/OS version.

Terminal screen read = SNAP folder on Drive, not live mirror.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OPTIONAL: share Downloads with Linux
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Chrome Files app → Downloads → right-click →
"Share with Linux" (if you see it)

Then screenshots may appear at:
  /mnt/shared/MyFiles/Downloads/

Say SNAP after that — CAPTN will check both paths.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WORDS: COPY · SNAP · PLUG · LIVE · EYES
