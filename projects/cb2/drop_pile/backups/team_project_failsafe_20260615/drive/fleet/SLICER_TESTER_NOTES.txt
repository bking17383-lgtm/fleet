# Video Slicer — tester feedback (2026-06-12)

Brian tested. Gaps vs Lester design — **fixed on penguin**, still need from Lester:

## Fixed now (Big Daddy)
- B/W border frame + placeholder logo SVG (replace with Lester custom asset)
- Permission gate before scan ("pick local videos" — no silent scan)
- Local device flow: pick → upload → catalog list → search → play local uploads
- **Copy for social** button (clipboard paste text + clip share)
- Cloud button labeled coming soon (Google Drive — next pass)

## Still needs Lester / puppy
- **Custom logo file** on Drive (`.png` or `.svg`) — drop at `Drive/lester/slicer_logo.svg`
- **Google Drive cloud hook** — user's cloud videos (not built yet)
- **GROQ_API_KEY** on server for real transcription (files save without it; search limited)

## Test again
Phone URL in `SLICER_PHONE_URL.txt` on Drive (refresh tunnel if dead).

Steps:
1. Open app → see permission screen + b/w border
2. Tap **Allow — pick local videos** → choose a short phone video
3. Wait for catalog row to appear
4. Search a word from the video (or any word if transcribed)
5. Tap **Copy for social** → paste in Messages/Facebook

Report bugs to Big Daddy. Marketing/landing = Lester track (parallel).
