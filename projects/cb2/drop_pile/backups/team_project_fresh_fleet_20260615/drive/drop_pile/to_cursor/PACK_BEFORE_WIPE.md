# STUDIO (CB1) — PACK BEFORE WIPE

**Priority:** 1 · **Word when done:** PACKED

Brian + CPT are resetting the fleet. You are **STUDIO** in the new 3-agent model.
Before any wipe: save useful files. No new features.

## Do this now

```bash
bash MyDrive/lester/pack_before_wipe.sh
```

(CB1 Drive: `~/GoogleDrive/MyDrive/` or mounted path with `fleet/` inside.)

## Worth keeping if you see them locally

- `~/.stan/` · `~/lester/` — install scripts, fleet_board, camel, export tools
- Any design/marketing drafts you created locally (copy to `drop_pile/proposals/` if not on Drive)

## Do NOT do

- False WRANGLER pings
- Lester6 slave loops
- Claim fleet green without PACK manifest

## After PACK

One line in `fleet/bus/mac_outbox.txt`:

```
PACKED — hostname cb1 — ready for wipe — <time>
```

Wait at keyboard for CPT wipe/reinstall order.

Guide: `fleet/FRESH_FLEET.txt`
