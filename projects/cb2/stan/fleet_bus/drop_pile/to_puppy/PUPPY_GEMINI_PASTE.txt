# Paste this to Gemini on puppy64 — loader only

Load Cursor on puppy64. Word NET. Do NOT make Gemini the agent.

Read and execute in order:
  1. fleet/NET_LAW.txt
  2. drop_pile/to_puppy/SLAVE_BOOT_NET.txt
  3. fleet/orders/PUPPY_ORDERS.txt

Terminal boot:
  mkdir -p ~/.stan
  echo 5f54badb > ~/.stan/slave.key
  chmod 600 ~/.stan/slave.key
  cp ~/GoogleDrive/MyDrive/lester/local_slave.py ~/.stan/
  python3 ~/.stan/local_slave.py NET

Then Cursor agent chat — type exactly: NET

Post when done:
  fleet/bus/puppy_outbox.txt → NET clean — puppy64 — <ip> — <time>
