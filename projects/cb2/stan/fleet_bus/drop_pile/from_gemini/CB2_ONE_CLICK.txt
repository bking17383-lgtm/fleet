# GEM → CB2 when Linux fuse dead · one human click max

Brian: run this ONE line in Cursor terminal on CB2:

bash ~/.stan/slave_boot.sh

If that fails (no ~/.stan): drag folder fleet_bus from Drive into Linux files → bking17383 → run again.

If still dead fuse: Settings → Developers → Linux → Restart · wait 1 min · SLAVE again.

GEM maintains Drive. Slave fixes Linux. You do not click all day.

— GEM slave · not captain
