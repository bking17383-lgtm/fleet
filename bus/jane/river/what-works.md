# WHAT WORKS — Jane river living list
# Proven only. Kids append via: ./scripts/jane-river.sh works "<bullet>"
# Last seed: forge/hm1 2026-08-15

- Git (`bking17383-lgtm/fleet` at ~/fleet) is the single library. Drive MCP needsAuth — Disk river is dark.
- Cloudflare named tunnel + hitme.dev PATH urls (/goal /daddy /george /bunny /projects). Not subdomain cosplay.
- Cold start: clone/pull → get-keys.sh if needed → agent runs heartbeat (Brian never pastes).
- Live proof: ./scripts/live.sh (pull + fresh vs STALE + order + last 5).
- Presence: ./scripts/active.sh <name> when Brian is on this seat. Asleep = free git heartbeat only.
- Heartbeat: ./scripts/heartbeat.sh <name> "<doing>" → bus/status/<name>.md
- Jane river: ./scripts/jane-river.sh drop <who> "<what>" → bus/jane/river/HEAD.md (Jane reads HEAD).
- New box onboard: ./scripts/onboard-box.sh <fleet_id> <agent> — measure, hardware card, river drop.
- forge/hm1 measured: KVM worker, hostname cursor, 4 CPU / 16G / Ubuntu 24.04. Card: bus/hardware/hm1.md
- Jane/kids copy path: bus/jane/ and bus/jane/river/
- Stamps or silence: stamp in git. No stamp = do not claim it.
- Identity check: bus/IDENTITY_CHECK.md — ask the question, never print the answer.
- Output to Brian: ~4 lines, one thing; agent runs every command.
- Six bookmarks only: bus/bookmarks.txt (all hitme.dev paths).
- Kids fetch mail: ./scripts/kids-fetch-mail.py (independent kid). Git river PASS on hm1. IMAP 993 reachable. Inbox needs ~/.stan/gmail.env on the box — never in chat.
- cb2 cloud seat card bus/hardware/cb2.md (Brian seated 2026-08-19)
- Kids restamp NOW on Jane river
