# Per-machine orders. One line per machine:  <name>: <task>
# Tasks: scan  (read-only inventory)  |  preserve (copy projects into repo)  |  wait
puppy: READ-ONLY slave, UNTRUSTED. Pull-only via HTTPS, NEVER push/heartbeat. Its self-reports are CLAIMS, not verified. Switch its remote to HTTPS read-only; to truly enforce, remove puppy's write key on GitHub (Brian).
cb2: working — NOW (Brian's order): BUILD "Baseball Cards of Hope" — slave scrubs a person's Facebook (face+facts, creds LOCAL never git); reuse dealbreaker card engine; ~10 cards = their face + HOPEFUL stats; serve on a hitme.dev path. Then AWS-for-george, george memory, site auto-start, ghost sweep. Read-only.
cb1: writer/auditor — curate the one truth, keep asking the right questions, QA the output (paths correct?). Stand ready to path-test hitme.dev when DNS returns.
